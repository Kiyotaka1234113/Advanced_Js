# AI Usage Report
**Student:** Жанторе (Жаник)
**Assignment:** Lab 13 - React Component Patterns

### 1. Tool Used
Google Gemini

### 2. Prompts Used
* "Помоги распознать, восстановить и исправить синтаксические ошибки в коде из PDF для Lab 13: React Component Patterns."
* "Напиши правильную структуру проекта и подготовь README.md в соответствии с требованиями 'Git Discipline' на два таска."
* "Подготовь AI Report по правилам AI Usage Policy."

### 3. How I modified and verified the code
* **Анализ и распределение:** Я изучил сгенерированный код и вручную распределил его по строгой структуре папок (`task1` для HOC и `task2` для Hooks), инициализировав два независимых проекта через Vite.
* **Тестирование (Verification):** Запустил локальные серверы (`npm run dev`) для обеих частей. В Task 1 я вручную протестировал работу `ThemeProvider` и переключение светлой/темной тем через UI. В Task 2 я проверил, как хук `useLocalStorage` сохраняет данные при перезагрузке страницы, и убедился, что таймеры `useDebounce` отрабатывают корректно.
* **Рефакторинг стилей и импортов:** Убедился, что все пути импортов соответствуют моей локальной структуре файлов, и внес мелкие правки в стили для корректного отображения компонентов.

### 4. What I learned
* **Higher-Order Components (HOCs):** Я на практике увидел, как паттерн HOC (на примере `withTheme`) решает проблему *prop drilling*. Теперь я понимаю, как оборачивать базовые UI-компоненты, чтобы "инжектировать" в них нужные свойства из Контекста, оставляя сами компоненты чистыми (stateless).
* **Custom Hooks & Performance:** Я понял, как абстрагировать сложную бизнес-логику и работу с сетью. Особенно ценным было изучение отмены асинхронных запросов через `AbortController` (чтобы избежать утечек памяти при размонтировании) и реализация системы кеширования в памяти для хука `useFetch` (паттерн SWR).